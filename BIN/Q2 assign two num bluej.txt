 Q2.WAP to find the sum,product,quotient and remainder when a=50 and 
b=10
public class Q2_assign_two_num
{
public static void main()
{
int a=50,b=10,sum,diff,quot,rem;
sum=a+b;
diff=a-b;
quot=a/b;
rem=a%b;
system.out.println("the sum of two number are "+sum);
system.out.println("the difference of the two number are"+diff);
sytem.out.println("the quotient of the two number are"+quot);
system.out.println("the remeinder of the two number are"+rem);
}
}